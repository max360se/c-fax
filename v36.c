/* unosite cijele brojeve sve dok se ne unese 0
program ispisuje sumu svih unesenih brojeva, ispisuje koliko brojeva je
uneseno te ispisuje prosjek unesenih brojeva (aritmeticka sredina, srednja vrijednost, average) */

#include <stdio.h>

main()
{
	int b;

	b = 0;
	while ((b = getchar()) != EOF){
		b = getchar();
	}
}

